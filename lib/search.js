

function search(dictionary, userChoice){
  var keys = Object.keys(dictionary);
  var searchType = {
    "exact":(keys, searchTerm)=>{
      for (let i =0; i < keys.length; i++) {
        if ()
      }
    },
    "partial":,
    "begins":,
    "ends":
  }
}

module.exports = search;
