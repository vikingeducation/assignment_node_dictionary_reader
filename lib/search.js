const search = {

  runSearchByType: (integer) => {
    let valid = [1, 2, 3, 4];
    if (valid.indexOf(integer) != -1) {
      switch(integer) {
        case 1:
          
        case 2:
        case 3:
        case 4:
      }
    }
  },



};

module.exports = search;
