const FS = require('fs');

module.exports = {
  //lists files in data/
  listDataFiles(){
    return new Promise((resolve) => {
      FS.readdir(path = './data', (err, data) => {
        if (err) { throw err };
        resolve(data);
      });
    });
  },

  loadDataFile(path){
    return new Promise((resolve) => {
      FS.readFile('./data/' + path, 'utf8', (err, data) => {
        if (err) { throw err };
        resolve(data);
      });
    });
  }
//
};